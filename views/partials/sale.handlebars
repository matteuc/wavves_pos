<li class="list-group-item d-flex justify-content-between align-items-center">
    <strong>Transaction #{{id}}</strong> <span class="text-muted">{{sale_date}}</span>
    <span class="badge badge-primary badge-pill">${{sale_total}}</span>
  </li>